import styled from 'styled-components';

export const Content = styled.div`
    position: relative;
background: ${(props)=>props.theme.gd};
padding: 1.5rem 2rem;
margin-left: 0;
margin-right: 0;
border-radius: 10px;
/* box-shadow: var(--light-shadow); */
transition: 0.5s;
`